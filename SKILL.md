---
name: testcase-generator
description: 测试用例生成器 - 基于等价类划分和边界值分析理论,按测试点(POINT)分批生成高质量测试用例,输出为 Markdown 格式。当用户执行 /testcase-gen 命令或需要生成测试用例时使用。
allowed-tools: Read, Write, Bash, Glob
---

# 测试用例生成器(testcase-generator)

## 1. 目标

根据测试点生成结构化测试用例，覆盖正向、反向、边界场景。

## 2. 输入输出

- **输入**：需求文档（.docx/.md/.txt）或 `{BASE_DIR}/plan.md`、`CLAUDE.md`
- **说明**：
  - 若提供需求文档 → 执行 Phase 0（5维度分析）自动生成 `{BASE_DIR}/plan.md`，再生成用例
  - 若 `{BASE_DIR}/plan.md` 已存在 → 跳过 Phase 0，直接生成用例
- **输出**：`test-case-{需求名}/{ITEM}/cases.md`（同一 ITEM 的所有 POINT 合并到一个文件，**不要一个 POINT 一个文件**）、`test-case-{需求名}/all_cases.md`、`test-case-{需求名}/all_cases.xmind`

## 2.1 Token 输出节流（必读）

为降低生成阶段的 token 消耗，遵守以下硬性规则：

1. **文件粒度 = ITEM，不是 POINT**：同一 ITEM（或嵌套结构下的同一叶子子 ITEM）下的所有 POINT 合并到一个 `cases.md` 文件，文件内用多个 `###` 区分 POINT。禁止 `POINT1.1.md` / `POINT1.2.md` 这种一 POINT 一文件的写法。
2. **Phase 0 五维度分析在思考中完成**：不要把"维度1：…维度2：…复杂度评分=…"这类中间推导写成对话正文。**只输出最终 plan 条目**。
3. **不做逐文件 validate**：所有 ITEM 写完后只跑一次整体 `validate.py {BASE_DIR}/ --check-duplicates`，跳过 Step 7 的单文件校验。
4. **plan.md 仅表格化条目**：不写功能描述段落、不写复杂度推导过程，只写 `### {功能名} [复杂度N，扁平/嵌套]` + POINT 行。
5. **自检清单内化**：Phase 0 / 生成阶段的自检清单不要逐条复述到对话，仅在**有未通过项时**输出未通过的那几条。

## 3. 核心原则

1. **策略指导**：提供判断标准，而非强制数量
2. **场景驱动**：基于实际场景复杂度生成用例
3. **数据具体**：测试数据必须具体，不用占位符
4. **可验证性**：预期结果必须明确可验证

---

## 触发条件

- 用户执行 `/testcase-gen` 命令
- 满足以下任一条件：
  - `{BASE_DIR}/plan.md` 已存在（跳过 Phase 0，直接生成用例）
  - 用户提供了需求文档（执行 Phase 0，自动生成 plan.md 后再生成用例）

---

## 4. 核心理论

### 4.1 等价类划分法

将输入数据划分为若干等价类,从每类中选取代表性数据测试。

**应用示例**:
```
POINT: 用户名密码登录

输入项: 用户名、密码

用户名等价类:
  有效: [6-20字符,字母数字下划线] → "test_user123"
  无效: [<6] "ab" | [>20] "verylongusername12345" | [特殊字符] "user@name" | [空] ""

密码等价类:
  有效: [8-20字符,含大小写+数字] → "Test1234"
  无效: [<8] "Test12" | [缺大写] "test1234" | [空] ""

生成原则: 一次测试一个等价类,其他保持有效
```

### 4.2 边界值分析法

边界是最容易出错的地方。

**边界三点法**: 离点(min-1) | 上点(min) | 内点(中间值) | 上点(max) | 离点(max+1)

**应用示例**:
```
用户名长度 6-20 字符:
- 离点-下: 5字符 "abcde" → 失败
- 上点-下: 6字符 "abcdef" → 成功
- 内点: 13字符 "test_user_123" → 成功
- 上点-上: 20字符 "test_user_1234567890" → 成功
- 离点-上: 21字符 "test_user_12345678901" → 失败
```

### 4.3 用例数量判断

不强制数量，根据场景复杂度自适应调整。

**判断标准**：
- 输入项数量：输入项越多，用例越多
- 业务规则复杂度：规则越复杂，用例越多
- 风险等级：Critical 和 High 需要更多用例
- 测试关注点：关注点越多，用例越多

---

## 5. 用例格式规范

### 5.1 XMind 导入格式

生成的用例采用 XMind 导入格式，支持直接导入外部测试管理系统。

**层级结构**：
- `##` → ITEM（一级分组，对应功能模块；支持嵌套，详见 5.7）
- `###` → POINT（二级分组，对应测试点）
- `####` → 测试用例（节点标题前加 `tc` 声明）

**ITEM 嵌套表示**：当一个大模块下还有多个子模块时，在 `##` 标题里用 ` / ` 分隔层级，例如 `## 远程办公 / Offer与合同模板`。XMind 导入端会自动把每段拆成一级主题，无需手写 `###` 子标题（`###` 仍然只用于 POINT）。
- `* pc: 内容` → 前置条件（测试用例的子节点）
- `* 步骤描述` → 操作步骤（测试用例的子节点）
- `    * 预期结果` → 预期结果（步骤的子节点，4空格缩进）

**格式示例**：

```markdown
## 模块名

### POINT 1.1 测试点名

#### tc-P1-功能测试: 验证xxx
* pc: 前置条件描述
* 步骤1描述
    * 预期结果1
* 步骤2描述
    * 预期结果2.1
    * 预期结果2.2

#### tc-P2-功能测试: 验证yyy
* 步骤1描述
    * 预期结果1
```

### 5.2 测试用例节点声明规则

用例标题前必须用 `tc` 声明，支持以下组合：

| 声明格式 | 含义 | 示例 |
|---------|------|------|
| `tc:` | 无优先级和类型 | `#### tc: 验证xxx` |
| `tc-P1:` | 声明优先级 P1 | `#### tc-P1: 验证xxx` |
| `tc-功能测试:` | 声明类型为功能测试 | `#### tc-功能测试: 验证xxx` |
| `tc-P1-功能测试:` | 同时声明 P1 + 功能测试 | `#### tc-P1-功能测试: 验证xxx` |
| `tc-P2-性能测试:` | 同时声明 P2 + 性能测试 | `#### tc-P2-性能测试: 验证xxx` |

**优先级说明**：
- P1：核心功能正向流程
- P2：基本功能正向流程
- P3：核心功能异常场景
- P4：边界条件
- P5：低频场景

### 5.3 测试类型枚举（仅2种）

```
功能测试
性能测试
```

**重要**：
- 安全测试、边界测试、异常场景测试 → 统一归为 **功能测试**
- 只有性能相关场景（响应时间、并发、压力）才使用 **性能测试**
- 默认使用功能测试

### 5.4 步骤与预期结果格式

- **步骤**：`* 步骤描述`（无缩进，直接写操作动作）
- **预期结果**：`    * 预期结果`（4空格缩进，作为步骤的子节点）
- 每个步骤可以有多个预期结果，多个预期结果会被系统自动合并
- 最多支持 20 个步骤，超出会被忽略

```markdown
* 在方案名称输入框中输入「季度人才盘点2024」
    * 输入框显示已输入的文字
* 点击「保存」按钮
    * 页面提示「保存成功」
    * 方案列表中出现新创建的方案条目
```

### 5.5 前置条件格式

- 前置条件必须是测试用例的直接子节点
- 必须使用 `pc:` 声明
- 格式：`* pc: 前置条件内容`

```markdown
#### tc-P1-功能测试: 验证xxx
* pc: 管理员账号已登录；系统中已存在至少一个方案
* 步骤1
    * 预期结果1
```

### 5.6 测试数据要求

必须具体，不用占位符：

**正确**：`输入方案名称「季度人才盘点2024」`
**错误**：`输入{方案名称}`

### 5.7 目录结构

目录名格式：`test-case-{需求名}/`，其中 `需求名` 在 Phase 0 或 Step 1 确认。

**序号前缀约定（强制）**：所有目录名必须以 `NN-` 开头（两位数字 + 中划线），按需求文档中**该层级出现的顺序**编号。`merge_cases.py` 输出 `## ITEM` 路径时会自动剥离 `NN-` 前缀，最终用例标题保持干净。

为什么强制：文件系统按字典序排目录，没有前缀就只能字母序，最终 all_cases.md 和 XMind 里的功能顺序与需求文档无关，下游需要人工调序。前缀是让物理目录顺序天然 = 文档顺序的最简单做法。

**目录结构示例**（一级章节 + 嵌套功能 + 叶子子 ITEM 的三层组织）：

```
test-case-{需求名}/
├── plan.md
├── 01-方案管理/                      # 需求文档「一、方案管理」
│   ├── 01-新建方案/                  # 文档第1个功能
│   │   └── cases.md
│   ├── 02-方案列表/                  # 文档第2个功能（自身复杂度高，再嵌套）
│   │   ├── 01-列表展示与筛选/
│   │   │   └── cases.md
│   │   ├── 02-操作按钮权限矩阵/
│   │   │   └── cases.md
│   │   └── 03-数据可见范围/
│   │       └── cases.md
│   └── 03-复制方案/
│       └── cases.md
├── 02-计划管理/                      # 需求文档「二、计划管理」
│   ├── 01-创建计划/
│   │   ├── 01-基本信息与环节配置/
│   │   │   └── cases.md
│   │   └── 02-时间配置约束/
│   │       └── cases.md
│   └── 02-批量人员管理/
│       └── ...
├── all_cases.md
└── all_cases.xmind
```

合并后 ITEM 路径示例：`## 方案管理 / 方案列表 / 列表展示与筛选`（前缀已剥离）。

**何时启用嵌套**：当一个业务领域下有 ≥3 个独立功能群（每个功能群自身又能细分多个 POINT）时，用子目录把它们组织在同一父 ITEM 下，避免出现 `远程办公-A` / `远程办公-B` / `远程办公-C` 这类被拍平的并列 ITEM。

**`cases.md` 文件内结构**：文件头一行 `## {目录相对路径}`（目录段用 ` / ` 拼接，可保留或省略 NN- 前缀，merge 时都会被剔除），随后每个 POINT 用 `### POINT X.X 名称` 起头，下面跟该 POINT 的所有 `#### tc-…` 用例。`merge_cases.py` 会自动剔除 `##` 行并按目录路径归并。

### 5.8 合并规则

**必须使用脚本合并**，不要手动拼接文件：

```bash
python3 .claude/skills/testcase-generator/scripts/merge_cases.py \
  "{BASE_DIR}/" \
  -o "{BASE_DIR}/all_cases.md" \
  -t "{需求名}"
```

脚本会自动完成以下规则：

1. 根节点标题为系统名称（不会被系统使用）
2. 按 ITEM（目录名）用 `##` 分组，**每个 ITEM 名称只出现一次 `##` 标题**
3. 每个 ITEM 下按 POINT（文件名）用 `###` 分组
4. 测试用例保持 `####` 层级
5. 各 POINT.md 文件中的 `## ITEM` 行会被脚本自动剔除，避免重复

---

## 6. 用例生成流程

### Phase 0: 逐功能独立深度分析（仅当直接提供需求文档时执行）

**触发条件**：用户提供了需求文档且 `{BASE_DIR}/plan.md` 不存在

**为什么要逐功能独立分析**：

若一次性扫描所有功能再输出 plan，模型会把注意力预算隐式摊薄到每个功能上——功能越多，每个功能分配到的深度越浅。逐功能独立分析可保证：**每个功能的拆分深度只由自身复杂度决定，与需求文档里有多少个功能无关。**

**Phase 0 执行方式：逐功能循环**

先从需求文档提取功能列表（一行一个），然后对列表中**每个功能单独执行以下分析**，分析时上下文里只放这一个功能的描述：

**5个维度的提取方法**：

| 维度 | 提取内容 | 问自己的问题 |
|------|---------|-------------|
| **维度1：功能操作** | 所有操作矩阵 | 有哪些增删改查和特殊操作？有批量操作/导入导出吗？ |
| **维度2：权限控制** | 角色×功能的权限矩阵 | 哪些操作是角色专属的？有隐式权限吗？ |
| **维度3：状态转移** | 业务对象的状态机 | 哪些对象有生命周期？哪些转移是非法的？ |
| **维度4：数据约束** | 每个字段的约束规则 | 长度/格式/必填/唯一性？有跨字段联动规则吗？ |
| **维度5：关联性** | 对象间的依赖关系 | 父对象变化影响子对象吗？删除时关联对象如何处理？ |

**POINT 风险等级默认规则**：
- 来自维度2（权限）、维度3（状态转移）、维度5（关联性）→ 默认 **High**
- 来自维度1（功能操作）、维度4（数据约束）→ 默认 **Medium**
- **覆盖规则**：若该 POINT 是核心正向主流程（用户最常走的路径，故障即阻断业务）→ 升级为 **High**，无视维度来源。维度只决定"测试关注点的种类"，业务关键性才决定风险等级。

**复杂度评分（针对当前功能，独立打分）**：

不同维度的测试成本差异很大——状态机和权限矩阵会指数级放大用例数，而单纯的字段约束基本是线性的。评分按此区分权重：

| 信号 | 计分 | 说明 |
|------|------|------|
| 维度2（权限）触发 | +2 | 每多一个角色都要重测一遍 |
| 维度3（状态转移）触发 | +2 | 状态×操作组合爆炸 |
| 维度5（关联性）触发 | +2 | 跨对象副作用难枚举 |
| 维度1（功能操作）触发 | +1 | 操作矩阵线性增长 |
| 维度4（数据约束）触发 | +1 | 字段校验线性增长 |
| 业务规则/条件分支数 ≥ 5 | +2；3-4 +1；≤2 +0 | 避免被字段数绑架 |
| 有约束的输入字段数 ≥ 8 | +2；4-7 +1；≤3 +0 | 同上 |
| 状态机有非法转移需要测 | +1 | 在维度3基础上再加 |
| 关联影响是级联删除/更新 | +1 | 在维度5基础上再加 |
| **交互项：维度2 × 维度3 同时触发** | +2 | 状态×角色组合爆炸，单独相加会低估 |
| **交互项：维度3 × 维度5 同时触发** | +2 | 状态变更触发跨对象副作用 |

**关键原则**：单纯"字段多但都是必填+长度"的功能不应进入嵌套——这类功能即使 10+ 字段也只是若干 POINT 的扁平展开。嵌套结构留给真正存在跨子模块差异的功能。

**根据总分决定 ITEM 结构**：

| 总分 | 结构决策 |
|------|---------|
| ≤ 4 | 扁平：1个 ITEM，直接挂 2-4 个 POINT |
| 5-8 | 扁平：1个 ITEM，挂 4-6 个 POINT |
| ≥ 9 | 嵌套：1个父 ITEM + 2-4 个子 ITEM，每个子 ITEM 挂 2-4 个 POINT |

**每个功能分析完成后，立即输出该功能的 plan 条目**（包含：ITEM 路径、子 ITEM 划分、POINT 列表、每个 POINT 的维度来源和预计用例数），再进入下一个功能。

**重要：5维度提取与复杂度评分必须在内部思考中完成，不要把"维度1：…维度2：…评分=…"等中间推导写成对话正文或 plan.md 段落**——只输出最终的 plan 条目（见下方格式）。

**Phase 0.5: 跨功能聚类（必做，独立分析完成后整体回看一次）**

逐功能分析阶段彼此不可见，会导致本应共享父 ITEM 的兄弟功能被拆成并列顶层目录。必须在所有功能独立分析完后整体回看一次，按以下规则聚类：

**聚类时不重新打分、不重新决定结构**——Phase 0 的复杂度分和扁平/嵌套决策是该功能"作为独立单元"时的属性，聚类只调整目录路径与父子关系。若聚类后某个父 ITEM 下子 ITEM 较多，单独子 ITEM 的复杂度分仍按 Phase 0 原值保留。

**为什么聚类不触发交互项重算**：Phase 0 的交互项（2×3、3×5）针对的是**同一功能内部**的多维度叠加；Phase 0.5 把兄弟功能挂到同一父 ITEM 下，并未把它们合并成同一个功能单元，因此各自的评分仍独立成立。

1. **合并条件 — 父目录名必须完全相同 OR 来自需求文档同一一级章节**：满足以下任一即可合并。
   - ✅ 父段名完全相同：`充值回传/A` 与 `充值回传/B` → 合并为 `充值回传/` 下的两个子 ITEM `A`、`B`。
   - ✅ 同一文档一级章节：需求文档"一、方案管理"下的 5 个独立功能（新建方案/方案列表/复制方案/方案详情/状态流转）→ 用 `方案管理/` 作为共同父目录。条件：该一级章节在文档中**显式标注**为章节标题（如"一、"/"1."/`#`），且 ≥3 个功能群归属其下。
   - ❌ 不可合并：`充值回传/A` 与 `充值/B` —— 父段名仅前缀相同且无共同章节，保持各自独立。
   - ❌ 不可合并：`充值台账改造/A` 与 `充值回传/B` —— 父段名不同，无章节归属，保持各自独立。
2. **除一级章节归属外，禁止做语义聚类**：不做包含、前缀、近义词等模糊匹配。一级章节归属必须以文档显式标注为准，不可凭语义猜测。
3. **禁止用连字符拍平**：永远不要把嵌套结构写成单层目录名 `父-子/`。同一父 ITEM 下的多个子模块必须用真正的目录嵌套（`父/子1/`、`父/子2/`），并在文件头 `## ITEM` 行用 ` / ` 拼接。
4. **章节级父目录可与功能级嵌套共存**（三层目录）：`方案管理/方案列表/列表展示与筛选/cases.md` 是合法的——一级章节是 `方案管理/`、嵌套功能是 `方案列表/`、叶子子 ITEM 是 `列表展示与筛选/`。merge_cases.py 已支持任意层级。

**输出**：将所有功能的 plan 条目合并（含 Phase 0.5 的路径调整），生成 `{BASE_DIR}/plan.md`。

**plan.md 格式要求**：每个功能条目必须包含以下信息，不允许出现"待补充"。**结构字段为硬约束**：

- 复杂度分 ≤ 8 → 必须 `结构: 扁平`，**禁止**出现 `子ITEM:` 行
- 复杂度分 ≥ 9 → 必须 `结构: 嵌套`，**必须**至少有 2 个 `子ITEM:` 行

```
### {功能名} [复杂度分: N，结构: 扁平/嵌套]
结构判定: {N} {≥9 / ≤8} → {嵌套 / 扁平}
评分构成: {加分项1} | {加分项2} | ... = N

# 扁平（≤8分）：直接挂 POINT，无 子ITEM 行
  - POINT X.X {测试点名} | 维度{N} | {High/Medium} | 预计{N}个用例

# 嵌套（≥9分）：必须有 子ITEM 行
子ITEM: {子模块名}
  - POINT X.X {测试点名} | 维度{N} | {High/Medium} | 预计{N}个用例
子ITEM: {子模块名}
  - POINT X.X {测试点名} | ...
```

**为什么必须写"结构判定"和"评分构成"**：把打分→阈值→结构的推理路径外显化，强制模型走完两步决策而不是凭直觉。

**validate_plan.py 在这两行上能做到什么**：
- ✅ 存在性检查：两行都必须出现
- ✅ 一致性检查：`结构判定:` 的分数和结构标签必须与 `[复杂度分: N，结构: X]` 头部一致
- ✅ 加和检查：`评分构成:` 末尾的 `= N` 必须等于复杂度分（加和不对说明评分项写错或漏写）
- ❌ 不检查评分项本身是否打对（这是模型自律范围，无法机器校验）

**格式容错**：`结构判定:` 行的比较符 `≥` `≤` 与 ASCII `>=` `<=` 等价；箭头 `→` 与 `->` 等价。冒号 `:` 与 `：` 等价。

**plan.md 片段示例（状态机功能）**：

```
### 订单状态流转 [复杂度分: 12，结构: 嵌套]
结构判定: 12 ≥ 9 → 嵌套
评分构成: 维度1+1 | 维度2+2 | 维度3+2 | 维度5+2 | 状态机非法转移+1 | 交互项2×3+2 | 交互项3×5+2 = 12

子ITEM: 用户侧状态操作
  - POINT 3.1 用户支付/取消触发的状态转移 | 维度3 | High | 预计5个用例
  - POINT 3.2 用户操作的非法转移拦截 | 维度3 | High | 预计3个用例
子ITEM: 商家侧状态操作
  - POINT 3.3 商家发货/确认触发的状态转移 | 维度3 | High | 预计4个用例
  - POINT 3.4 商家权限差异（普通客服 vs 店主） | 维度2 | High | 预计4个用例
子ITEM: 系统自动转移
  - POINT 3.5 超时/事件驱动的状态转移 | 维度3 | High | 预计3个用例
```

**plan.md 生成后必须执行机器化校验**（不仅靠思考自检）：

```bash
python3 .claude/skills/testcase-generator/scripts/validate_plan.py "{BASE_DIR}/plan.md"
```

脚本检查复杂度分与结构标签的一致性（≥9 必须嵌套且有子ITEM；≤8 必须扁平且无子ITEM）。**校验未通过禁止进入 Step 1**，必须先修正 plan.md。

**plan.md 生成后思考自检（仅当存在未通过项时，输出未通过的几条）**：
- 每个功能都已独立完成5维度分析、有复杂度评分和结构决策
- 每个 POINT 都有预计用例数标注；plan.md 中无"待补充"
- 维度1-5 的关注点都有对应 POINT（每模块每操作 / 每角色权限差异 / 每状态转移含非法 / 每字段约束 / 核心依赖异常）
- 已执行 Phase 0.5 跨功能聚类回看：父段名**完全相同**的兄弟功能已合并；前缀相同但父段名不同的保持独立；不存在 `父-子/` 拍平目录名

---

### Step 1: 确定输出目录

**确认需求名**（按以下优先级获取）：
1. 用户在命令中已指定 → 直接使用
2. 需求文档标题 / CLAUDE.md 中有明确名称 → 自动提取并告知用户
3. 无法推断 → 询问用户：「请提供需求名称（如：广告投放系统）」

确认后设定基础目录：`BASE_DIR = "test-case-{需求名}"`，后续所有文件路径均使用此前缀。

### Step 2: 准备上下文

```bash
cat {BASE_DIR}/plan.md
cat clarified-requirements/index.md
cat CLAUDE.md
```

### Step 3: 按 plan.md 的深度处方执行生成

**重要**：生成阶段不再自行判断每个 POINT 需要多少用例、用几层 ITEM——这些决策已在 Phase 0 里完成并写入 plan.md。本阶段的职责是**严格执行 plan.md 的处方**。

对每个 POINT，从 plan.md 读取：
- POINT 名称、风险等级、维度来源
- **预计用例数**（作为目标参考值，不是硬约束）
- 所属 ITEM 路径（决定文件写入哪个目录）

**预计用例数的使用方式**：
- 默认按预计数 ±1 生成
- **允许双向偏离**，但偏离 ≥ ±3 必须在该 POINT 的 cases.md 顶部写一行注释：
  - 上偏：`<!-- 实际N个，超出预计M个，原因：发现X等价类/边界值 -->`
  - 下偏：`<!-- 实际N个，少于预计M个，原因：X和Y等价类逻辑等价已合并 / 该字段无范围约束故无边界用例 -->`
- 上偏每条必须对应明确的等价类或边界值，不允许重复换皮
- 下偏必须给出"为什么少"的具体理由，不能是"觉得够了"
- 何时该停的整体判断（单 POINT 上限、总数预警）见 §10.5

> 若 plan.md 里没有预计用例数标注（如直接提供了已有 plan.md），则按 Step 6 的框架独立评估。

### Step 4: 识别输入项

- 列出所有输入项（字段、参数、操作等）
- 识别输入项的约束条件

### Step 5: 划分等价类和边界值

- **有效等价类**：符合规则的输入
- **无效等价类**：违反规则的输入
- **边界值**：临界点（最小值、最大值、临界值）

### Step 6: 设计测试用例

基于以下策略设计：

**核心思考框架**:

1. **输入分析**
   - 该场景涉及哪些输入项？
   - 每个输入有什么约束（长度、格式、范围）？

2. **等价类识别**
   - 有效等价类：正常情况下的典型值
   - 无效等价类：各种异常情况（空值、越界、格式错误）

3. **测试价值评估**
   - 这个等价类失败时，会暴露不同的缺陷吗？
   - 如果和其他等价类的测试逻辑相同，是否可以合并？
   - 测试关注点中提到的场景，如何在用例中体现？

4. **覆盖判断**
   - 有效流程至少有一个用例
   - 核心异常场景已覆盖
   - 边界条件根据实际情况选择

5. **用例设计**
   - 核心正向用例（P1）：最常见的成功场景
   - 基本正向用例（P2）：其他有效等价类
   - 核心异常用例（P3）：关键失败场景
   - 边界条件用例（P4）：边界值测试
   - 低频场景用例（P5）：不常见但需覆盖的场景

**用例质量标准**:
- 数据具体（"test123"，不要"正确用户名"）
- 预期结果可验证（"显示错误提示'用户名不能为空'"，不要"提示错误"）
- 命名规范（以"验证"开头）

### Step 7: 生成完毕后统一校验

**只做整体校验，不做逐文件校验**（节流：避免每写一个文件就跑一次脚本+解读日志）：

```bash
uv run .claude/skills/testcase-generator/scripts/validate.py "{BASE_DIR}/" --check-duplicates
```

仅在整体校验报错时分析日志并修正。

---

## 7. 示例

### 示例1：正确的用例（用户名密码登录）

```markdown
## 登录功能

### POINT 1.1 用户名密码登录

#### tc-P1-功能测试: 验证有效用户名和密码登录成功
* pc: 已注册用户test_user，密码Test1234；处于登录页面
* 输入用户名test_user
* 输入密码Test1234
* 点击登录按钮
    * 跳转到/home页面，顶部显示「欢迎，test_user」

#### tc-P3-功能测试: 验证用户名为空时登录失败
* pc: 处于登录页面
* 不输入用户名，输入密码Test1234
* 点击登录按钮
    * 停留在登录页，用户名输入框下方显示红色提示「用户名不能为空」

#### tc-P4-功能测试: 验证密码长度恰好8位时登录成功
* pc: 已注册用户test_user，密码Test1234
* 输入用户名test_user，输入8位密码Test1234
* 点击登录按钮
    * 登录成功，跳转到/home页面
```

### 示例2：状态机场景（维度3主导）

**POINT**：订单状态流转

```markdown
## 订单管理

### POINT 3.1 订单状态流转

#### tc-P1-功能测试: 验证待支付订单支付后流转到已支付
* pc: 已创建订单ORD001，状态「待支付」，金额99元
* 用户完成支付
    * 订单ORD001状态变更为「已支付」
    * 订单详情页显示支付时间

#### tc-P1-功能测试: 验证已支付订单发货后流转到已发货
* pc: 订单ORD001处于「已支付」状态
* 商家点击「发货」并填写物流单号SF1234567890
    * 订单状态变更为「已发货」
    * 用户端显示物流信息SF1234567890

#### tc-P3-功能测试: 验证已完成订单不能再次发货（非法转移）
* pc: 订单ORD001处于「已完成」状态
* 商家在后台尝试点击「发货」按钮
    * 「发货」按钮置灰不可点击
    * 接口返回错误「当前状态不支持此操作」

#### tc-P3-功能测试: 验证已取消订单不能恢复为待支付（非法转移）
* pc: 订单ORD001处于「已取消」状态，取消时间2小时前
* 用户在订单详情页查找恢复入口
    * 不存在「恢复订单」按钮
    * 直接调用恢复接口返回「订单已取消，请重新下单」

#### tc-P4-功能测试: 验证待支付订单30分钟未支付自动流转到已取消
* pc: 创建订单ORD002，状态「待支付」，创建时间为31分钟前
* 触发定时任务
    * 订单ORD002状态变更为「已取消」
    * 取消原因记录「超时未支付」
    * 释放占用的库存
```

**说明**：维度3场景必须覆盖（a）每条合法转移、（b）至少一条非法转移、（c）由系统自动触发的转移（超时/事件）。

### 示例3：综合场景（多输入项）

**POINT**: 广告账户列表-筛选功能

```markdown
## 广告账户管理

### POINT 2.1 账户列表筛选

#### tc-P1-功能测试: 验证单一条件筛选返回正确结果
* pc: 广告账户列表页面，已有10个账户
* 选择时间「近7天」
* 点击查询按钮
    * 列表刷新，仅显示近7天有数据的账户

#### tc-P1-功能测试: 验证多条件组合筛选返回正确结果
* pc: 广告账户列表页面，已有10个账户
* 选择时间「近7天」，输入名称「test」，选择状态「正常」
* 点击查询按钮
    * 列表刷新，仅显示同时满足3个条件的账户

#### tc-P3-功能测试: 验证时间范围超过365天时筛选失败
* pc: 广告账户列表页面
* 选择自定义时间，开始日期2023-01-01，结束日期2024-01-02
* 点击查询按钮
    * 显示提示「时间范围不能超过365天」
    * 列表不刷新

#### tc-P3-功能测试: 验证不存在的账户名筛选返回空列表
* pc: 广告账户列表页面
* 输入不存在的账户名「nonexistent123」
* 点击查询按钮
    * 列表刷新，显示「暂无数据」

#### tc-P2-功能测试: 验证重置按钮清空所有筛选条件
* pc: 已应用筛选条件
* 点击「重置」按钮
    * 所有筛选项恢复默认值
    * 列表显示全部账户

#### tc-P4-功能测试: 验证结束时间早于开始时间时提示错误
* pc: 广告账户列表页面
* 选择自定义时间，开始日期2024-01-02，结束日期2024-01-01
* 点击查询按钮
    * 显示提示「结束时间不能早于开始时间」
```

### 示例4：错误的用例

```markdown
#### tc-P1-功能测试: 验证登录成功
* pc: 用户已注册
* 输入用户名{username}，输入密码{password}
* 点击登录
    * 登录成功
```

**为什么错误**：
- 测试数据使用占位符（{username}、{password}）
- 预期结果不可验证（「登录成功」太模糊）

---

## 8. 检验流程

### Step 1: 全部生成完毕后整体校验（仅此一次）

```bash
uv run .claude/skills/testcase-generator/scripts/validate.py "{BASE_DIR}/" --check-duplicates
```

脚本只提供日志，不直接修改文件。**仅当报错时**分析日志并调整对应 `cases.md`。**不做单文件校验。**

### Step 2: 生成合并后的 all_cases.md 并导出 XMind

**先合并**（确保相同 ITEM 只出现一次 `##` 标题）：

```bash
python3 .claude/skills/testcase-generator/scripts/merge_cases.py \
  "{BASE_DIR}/" \
  -o "{BASE_DIR}/all_cases.md" \
  -t "{需求名}"
```

**再导出 XMind**：

```bash
python3 .claude/skills/testcase-generator/scripts/to_xmind.py \
  "{BASE_DIR}/all_cases.md" \
  -o "{BASE_DIR}/all_cases.xmind" \
  -t "{需求名}"
```

### Step 3: 生成质量报告

- 统计用例数量（按 ITEM、按优先级）
- 统计等价类覆盖率
- 统计边界值覆盖率
- 列出重复用例（如果有）

---

## 9. 检查清单

**自检在思考中执行，不要把清单逐条复述到对话；仅当存在未通过项时，输出未通过的几条。**

**生成前**：plan.md 已就绪（Phase 0 自检通过）；已读取 CLAUDE.md 业务背景。

**生成时**：用例数 ≈ plan.md 预计数（±1）；`tc-Px-功能测试:` 前缀；类型仅"功能测试"/"性能测试"；`* pc:` 声明前置；步骤无缩进、预期 4 空格缩进；数据具体无占位符；预期可验证；标题"验证"开头。

**治理阶段**：整体 validate.py 通过 → merge_cases.py 生成 all_cases.md → to_xmind.py 生成 xmind → 输出统计报告。

---

## 10. 脚本接口

### validate.py

```bash
# 单文件校验
uv run .claude/skills/testcase-generator/scripts/validate.py \
  --single "{BASE_DIR}/{模块}/{测试点}.md"

# 全局校验
uv run .claude/skills/testcase-generator/scripts/validate.py "{BASE_DIR}/"

# 全局校验 + 重复检测
uv run .claude/skills/testcase-generator/scripts/validate.py "{BASE_DIR}/" --check-duplicates

# 仅重复检测
uv run .claude/skills/testcase-generator/scripts/validate.py "{BASE_DIR}/" --duplicates-only
```

**校验项**:
- Schema格式
- 必填字段(优先级、测试类型、测试步骤、预期结果)
- 测试类型枚举(12种)
- 步骤编号连续性
- 步骤与预期结果数量一致性
- 等价类覆盖检查(`--check-equivalence`)
- 边界值覆盖检查(`--check-boundary`)
- 重复检测(`--check-duplicates`)：自动跳过"状态对比型并行用例"（步骤相似 ≥95% 且预期相似 ≤30% 的对，是状态机/权限矩阵刻意设计的对比组）

**目录扫描**：递归遍历 BASE_DIR 下任意层级的 `cases.md`（含三层嵌套 `章节/功能/子ITEM/cases.md`），与 merge_cases.py 行为一致。

### merge_cases.py

```bash
# 合并所有 POINT.md 到 all_cases.md（相同 ITEM 只出现一次 ## 标题）
python3 .claude/skills/testcase-generator/scripts/merge_cases.py \
  "{BASE_DIR}/" \
  -o "{BASE_DIR}/all_cases.md" \
  -t "{需求名}"
```

**行为**：
- 递归遍历 BASE_DIR 下任意层级的目录，按"包含 .md 的目录的相对路径"分组
- 自动剔除各 POINT.md 中多余的 `## ITEM` 行
- 同一目录下的 POINT 归入同一 `##` 块；多级目录用 ` / ` 拼接（如 `## 远程办公 / Offer与合同模板`）
- 扁平结构（一层目录）和嵌套结构（多层目录）可在同一 BASE_DIR 内共存

### to_excel.py

```bash
# 从目录导出
uv run .claude/skills/testcase-generator/scripts/to_excel.py "{BASE_DIR}/" -o "{BASE_DIR}/export.xlsx"

# 从单个文件导出
uv run .claude/skills/testcase-generator/scripts/to_excel.py "{BASE_DIR}/{模块}/{测试点}.md" -o "output.xlsx"
```

### to_xmind.py

```bash
# 从 all_cases.md 导出（推荐）
uv run .claude/skills/testcase-generator/scripts/to_xmind.py \
  "{BASE_DIR}/all_cases.md" \
  -o "{BASE_DIR}/all_cases.xmind" \
  -t "{需求名}"

# 从目录导出（自动查找 all_cases.md，否则遍历子目录）
uv run .claude/skills/testcase-generator/scripts/to_xmind.py \
  "{BASE_DIR}/" \
  -o "{BASE_DIR}/all_cases.xmind" \
  -t "{需求名}"
```

---

## 10.5 何时该停（边际收益判断）

生成阶段每写完一个 POINT，问自己：

1. **当前 POINT 已有的用例是否覆盖了"用例失败时能定位到不同缺陷"**？若新增一个用例失败的现象与已有用例完全相同，停止扩张。
2. **该 POINT 用例数是否已达 12（硬上限）**？达到立即停。回 plan.md 拆 POINT，不要在单文件里堆。
3. **整个需求总用例数预警**（按 plan.md 中 POINT 总数 P 计算期望）：
   - 期望区间 = `[P × 3, P × 6]`（每个 POINT 平均 3-6 个用例）
   - 实际总数 < P×3：复盘是否分析阶段高估了 POINT 数量，或生成阶段偷懒
   - 实际总数 > P×6：必须停下回看 plan.md，复盘 POINT 粒度过粗（应拆）或风险等级普遍标高
   - 不设绝对值兜底——大需求天然用例多（67 POINT 产出 ~240 用例属正常），P×6 已是足够紧的上界

**禁止用"再补几个边界用例显得全面"作为扩张理由**——边界用例只在该字段确实有范围/格式约束时生成，无约束的字段不写无意义边界。

---

## 11. 异常处理

| 错误 | 处理方式 |
|------|----------|
| 单文件校验失败 | 重试3次，仍失败则跳过并记录 |
| 单 POINT 用例数 > 12 | 强制停下，回 plan.md 把该 POINT 拆成 2 个（详见 §10.5） |
| 总用例数超过 P×6 | 强制停下回看 plan.md，复盘 POINT 粒度或风险等级（详见 §10.5） |
| 缺少边界用例 | 警告并建议补充 |
| 缺少无效等价类 | 警告并建议补充异常场景 |
| 步骤编号不连续 | 报错并要求修正 |
| 测试类型不在枚举 | 报错并要求修正 |
| 步骤与预期数量不一致 | 报错并要求修正 |
| 导出失败 | 检查openpyxl依赖 |

---

## 12. 停止点

✅ **用例生成完成**

**产物**:
- `test-case-{需求名}/{ITEM}/cases.md` - 该 ITEM 下所有 POINT 的用例（用 `### POINT X.X` 分隔）
- `test-case-{需求名}/{父ITEM}/{子ITEM}/cases.md` - 嵌套结构下的叶子 ITEM 用例
- `test-case-{需求名}/all_cases.md` - 所有用例汇总（Markdown / XMind 导入格式）
- `test-case-{需求名}/all_cases.xmind` - XMind 思维导图文件（可直接用 XMind 打开）

**质量保证**:
- 基于等价类理论生成
- 边界值全覆盖
- 用例数量合理（基于场景复杂度）

**统计示例**:
```
📊 生成统计:
  - 测试项: 8个
  - 测试点: 20个
  - 测试用例: 75个

  覆盖率:
  - 有效等价类: 100% (每POINT至少1个)
  - 无效等价类: 80% (主要异常全覆盖)
  - 边界值: 90% (所有范围型输入)

  用例分布:
  - P1(核心正向): 20个(27%)
  - P2(基本正向): 15个(20%)
  - P3(核心异常): 25个(33%)
  - P4(边界条件): 12个(16%)
  - P5(低频场景): 3个(4%)
```

**下一步**:
- 人工审核测试用例
- 导出为 Excel 格式（可选）
- 根据业务理解调整用例
